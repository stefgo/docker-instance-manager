# Docker Instance Manager - Client Configuration

# Identifier of this client. Issued by the server during registration and written here
# together with authToken — leave empty, do not set manually.
clientId:

# Log level for the client (debug, info, warn, error)
logLevel: "info"

# URL of the DIM Server (e.g., http://your-server:3000)
serverUrl:

# Authentication token for server communication
# Provided during initial registration and stored locally — do not set manually.
authToken:

# Secret for server-initiated (inbound) registration.
# Set this when the server connects to the client instead of the other way around.
# Must match the value entered in the server's "Add Outbound Client" dialog.
# Automatically removed from config after successful registration.
registrationSecret:

# Docker socket path (optional – auto-detected if not set)
# macOS (Docker Desktop): /var/run/docker.sock
# Linux:                  /var/run/docker.sock
dockerSocket: "/var/run/docker.sock"

# IPv4 addresses or CIDR networks the server may dial this agent from, checked on
# /ws/register and /ws/agent. Empty (the default) allows every address.
# It matters most for /ws/register, where the caller hands over the auth token this agent
# stores. The address checked is the socket peer: behind a reverse proxy list the proxy.
# The local web UI on the same port is not restricted by this.
# A wrong value can only be fixed on this host -- the connection you would use is refused.
allowedNetworks: []

# Show the status page at /status (default: true)
enableStatusPage: true

# Show the registration page at /register and accept POST /api/register (default: true)
# Registering requires the setup PIN printed to the agent's log on startup.
# Disable once the client is registered and no re-registration is expected.
enableRegisterPage: true
